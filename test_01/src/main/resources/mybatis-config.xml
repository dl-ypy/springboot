<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<settings>
		<!-- mybatis map空处理为null  -->
		<setting value="true" name="callSettersOnNulls"/>
		<!-- 日志 -->
		<setting name="logPrefix" value="mapper." />
	</settings>
	<!-- 配置数据库用户
	
	<properties resource="db.properties" />

	<plugins>
		缓存使用拦截器插件
		<plugin interceptor="com.bonc.intercept.CacheIntercept"></plugin>
	</plugins> -->
	
	<!--mapper映射 -->
	<mappers>
		<!--模板 -->
		<mapper resource="mybatisMapper/UserMapper.xml"/>
	</mappers>
</configuration>
